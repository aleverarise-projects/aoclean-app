import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const NewRequestScreen = () => {
  return (
    <View>
      <Text>NewRequestScreen</Text>
    </View>
  );
};

export default NewRequestScreen;

const styles = StyleSheet.create({});
